import 'dart:ui';

class AppColor {
  static const dark = Color(0xFF292F3F); //for bg
  static const light = Color(0xFF373E4E); //secodory bg
  static const darkest = Color(0xFF272A35); //for message container
  static const violet = Color(0xFFB347EA);
  static const orange = Color(0xFFF18303);
  static const blue = Color(0xFF03A9F1);
  static const green = Color(0xFF00AC83);
  static const lightblue = Color(0xFF837DFF);
}
